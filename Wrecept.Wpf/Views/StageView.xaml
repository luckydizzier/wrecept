<UserControl x:Class="Wrecept.Wpf.Views.StageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             KeyDown="OnKeyDown">
    <Grid Background="{DynamicResource StageBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="2">
            <Button Content="Számla" Command="{Binding ShowInvoiceEditorCommand}" Margin="2"/>
            <Button Content="Termékek" Command="{Binding ShowProductMasterCommand}" Margin="2"/>
            <Button Content="Beszállítók" Command="{Binding ShowSupplierMasterCommand}" Margin="2"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" />

        <TextBlock Grid.Row="2" Text="Állapotsor" Margin="2" />
    </Grid>
</UserControl>
