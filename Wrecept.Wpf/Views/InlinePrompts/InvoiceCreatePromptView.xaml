<UserControl x:Class="Wrecept.Wpf.Views.InlinePrompts.InvoiceCreatePromptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding ConfirmAsyncCommand}" />
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" />
    </UserControl.InputBindings>
    <Border Background="#F5F5DC" Padding="4" Margin="0,4,0,0" HorizontalAlignment="Center">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBox x:Name="NumberBox" Width="120" Text="{Binding EditableNumber}" Margin="0,0,4,0" />
            <TextBlock Text="{Binding Message}" VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
