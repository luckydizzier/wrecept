<UserControl x:Class="Wrecept.Wpf.Views.Controls.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource HeaderFooterBrush}" Padding="2">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding DateTime}"/>
            <TextBlock Grid.Column="1" Text="{Binding ActiveMenu}" Margin="8,0"/>
            <TextBlock Grid.Column="2" Text="{Binding FocusedElement}" Margin="8,0"/>
            <TextBlock Grid.Column="3" Text="{Binding Message}" Margin="8,0" />
            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Num" Margin="2,0" Visibility="{Binding IsNumLock, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Text="Caps" Margin="2,0" Visibility="{Binding IsCapsLock, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Text="Scr" Margin="2,0" Visibility="{Binding IsScrollLock, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
