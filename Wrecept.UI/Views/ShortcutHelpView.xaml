<Window x:Class="Wrecept.UI.Views.ShortcutHelpView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        KeyboardNavigation.TabNavigation="None"
        mc:Ignorable="d"
        Title="Shortcut Help" Height="300" Width="400"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource WindowForegroundBrush}">
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding EnterCommand}" />
        <KeyBinding Key="Escape" Command="{Binding EscapeCommand}" />
        <KeyBinding Key="Left" Command="{Binding LeftCommand}" />
        <KeyBinding Key="Right" Command="{Binding RightCommand}" />
        <KeyBinding Key="Up" Command="{Binding UpCommand}" />
        <KeyBinding Key="Down" Command="{Binding DownCommand}" />
    </Window.InputBindings>
    <StackPanel Margin="10">
        <TextBlock Text="F1 - Súgó" ToolTip="Gyorsbillentyűk súgója" />
        <TextBlock Text="Ctrl+1 - Fiókok" />
        <TextBlock Text="Ctrl+2 - Készletek" />
        <TextBlock Text="Ctrl+3 - Listák" />
        <TextBlock Text="Ctrl+4 - Karbantartás" />
        <TextBlock Text="Ctrl+5 - Kapcsolatok" />
    </StackPanel>
</Window>
